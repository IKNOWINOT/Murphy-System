# Synthetic Benchmark Runs Archive

These JSON files were produced on **2026-03-13** by the benchmark harness 
running **against synthetic in-code task lists**, NOT the real public 
τ-bench / AgentBench / ToolBench / Terminal-Bench / GAIA datasets.

The "agent" being evaluated also did not have a working LLM connection 
at the time — so the scores (1.0, 0.5, 0.0) reflect harness defaults, 
not actual model performance.

**These files MUST NOT be cited as evidence of Murphy's benchmark 
performance.** Any external claim must use real benchmark runs from 
2026-06-10 onward. See `/documentation/testing/benchmark_real/` for 
real results.

Archived by ship 28 on 2026-06-10 for truth hygiene.
