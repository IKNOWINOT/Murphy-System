# Ship 31z: ban IPs that repeatedly trip nginx rate limits.
# Triggers on 429 responses for /verify/, /api/webhook/, /api/chat.
[Definition]
failregex = ^<HOST>.*"(GET|POST|PUT|DELETE) /(verify|api/webhook|api/chat)[^"]*" 429 
ignoreregex =
