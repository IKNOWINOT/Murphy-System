apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: murphy-data
  namespace: murphy-system
  labels:
    app: murphy-system
    component: storage
    environment: production
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
