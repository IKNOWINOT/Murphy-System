# Kiren — aionmind_core-based Bot (Self-Contained)
Purpose: Planner/creator; synthesizes actionable plans and drafts.

This folder is **self-contained** and based on a minimal `internal/aionmind_core` core so you can drop it into the repo and simply add to registry. It adheres to the canvas **Bot standards** prompt.

Register:
- run: `src/clockwork/bots/kiren/kiren.ts::run`
- ping: `src/clockwork/bots/kiren/rollcall.ts::ping`
