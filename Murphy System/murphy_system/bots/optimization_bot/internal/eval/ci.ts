export function wilson(p:number, n:number, z=1.96){ const denom=1+z*z/n; const center=(p + z*z/(2*n))/denom; const margin= z*Math.sqrt((p*(1-p)/n) + (z*z/(4*n*n)))/denom; return [Math.max(0,center-margin), Math.min(1,center+margin)]; }