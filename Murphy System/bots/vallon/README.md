# Vallon — aionmind_core-based Bot (Self-Contained)
Purpose: Governor; budgets, prioritizes, throttles, and schedules.

This folder is **self-contained** and based on a minimal `internal/aionmind_core` core so you can drop it into the repo and simply add to registry. It adheres to the canvas **Bot standards** prompt.

Register:
- run: `src/clockwork/bots/vallon/vallon.ts::run`
- ping: `src/clockwork/bots/vallon/rollcall.ts::ping`
