# Veritas — aionmind_core-based Bot (Self-Contained)
Purpose: Validator; checks facts, compares, suggests citations.

This folder is **self-contained** and based on a minimal `internal/aionmind_core` core so you can drop it into the repo and simply add to registry. It adheres to the canvas **Bot standards** prompt.

Register:
- run: `src/clockwork/bots/veritas/veritas.ts::run`
- ping: `src/clockwork/bots/veritas/rollcall.ts::ping`
