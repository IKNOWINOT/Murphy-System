# Database

The `database` package provides the database connection pool, migration
helpers, and session factory used across the Murphy System.

## Package Contents

The package exposes session factory utilities and ORM base classes.
See `src/database/__init__.py` for exported symbols.
