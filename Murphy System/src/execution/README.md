# Execution

The `execution` package provides document-generation capabilities within
the execution pipeline — transforming structured data and templates into
final output documents.

## Key Module

| Module | Purpose |
|--------|---------|
| `document_generation_engine.py` | `DocumentGenerationEngine` — renders Jinja2 templates with execution results |
