# Murphy System — Matrix Bridge Dependencies
# Copyright © 2020 Inoni Limited Liability Company
# Creator: Corey Post
# License: BSL 1.1
#
# These packages are OPTIONAL additions to the core Murphy System runtime.
# The matrix_bridge package degrades gracefully when matrix-nio is absent
# (using MockMatrixClient instead of the real nio client).
#
# Install with:
#   pip install -r src/matrix_bridge/requirements.txt

# Matrix SDK (async client with optional end-to-end encryption)
# E2E encryption support requires the [e2e] extra:
#   pip install "matrix-nio[e2e]>=0.20.0"
matrix-nio>=0.20.0

# Async HTTP client used by matrix-nio and optional REST integrations
aiohttp>=3.9.0

# Cryptography for E2E encryption (pulled in by matrix-nio[e2e])
# Listed explicitly for auditability
cryptography>=41.0.0

# Optional: YAML support for room-topology config files
pyyaml>=6.0.0
