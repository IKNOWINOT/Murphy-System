        window.onload = () => {
            initPhaseIndicator();
            updatePhaseIndicator();
            updateMetrics();
            renderStateTree();
            renderSwarms();
            renderArtifacts();
            renderGates();
            renderConstraints();
            
            terminalOutput('system', 'Murphy System Backend Integrated Runtime v1.0');
            terminalOutput('system', 'Type "help" for available commands');
            terminalOutput('system', 'Click INITIALIZE to start the system');
        };
    </script>
</body>
</html>