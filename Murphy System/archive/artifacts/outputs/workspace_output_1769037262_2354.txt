        const API_BASE = '';
        
        let connected = false;
--
            terminalOutput('system', `Testing connection to Murphy System backend at ${API_BASE}...`);
            
            try {
                const response = await fetch(`${API_BASE}/api/status`);
                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}`);
--
                const response = await fetch(`${API_BASE}/api/initialize`, {
                    method: 'POST',
                    headers: {
--
                const response = await fetch(`${API_BASE}/api/status`);
                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}`);
--
                const response = await fetch(`${API_BASE}/api/states`);
                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}`);
--
                const response = await fetch(`${API_BASE}/api/agents`);
                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}`);
--
                const response = await fetch(`${API_BASE}/api/states/${selectedStateId}/evolve`, {
                    method: 'POST',
                    headers: {
--
            terminalOutput('system', `API Base: ${API_BASE}`);
            terminalOutput('system', '');
            terminalOutput('system', 'Instructions:');
