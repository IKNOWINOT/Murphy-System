# Kaia — aionmind_core-based Bot (Self-Contained)
Purpose: Meta-orchestrator; routes tasks and coordinates archetypes.

This folder is **self-contained** and based on a minimal `internal/aionmind_core` core so you can drop it into the repo and simply add to registry. It adheres to the canvas **Bot standards** prompt.

Register:
- run: `src/clockwork/bots/kaia/kaia.ts::run`
- ping: `src/clockwork/bots/kaia/rollcall.ts::ping`
