[pytest]
testpaths = tests
asyncio_mode = auto
# Suppress PytestCollectionWarnings for production source classes whose names
# start with "Test" (e.g. TestVector, TestVectorGenerator, TestModeExecutor).
filterwarnings =
    ignore::pytest.PytestCollectionWarning
