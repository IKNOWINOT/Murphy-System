"""Empty package marker for `tests/contracts/`.

This package holds invariants that are *contractual* (cross-module rules
that must hold true regardless of who edits which file). They run as part
of the normal pytest suite.
"""
